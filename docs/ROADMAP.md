# ROADMAP.md — CaucasHub Feature Backlog

Статусы: ✅ Готово | 🔄 В работе | 💤 Отложено (осознанно) | ❌ Отменено

---

## 💤 Отложено

### 3.4.8 — URL-параметры с фильтрами

**Описание:** При применении фильтров (город, тип кузова, диапазон цены) URL не обновляется. Поделиться ссылкой с активными фильтрами нельзя.

**Пример желаемого поведения:**
```
https://caucashub.ge/?from=Тбилиси&to=Батуми&type=ref
```

**Почему отложено:** Не является блокером для запуска. Ценность появляется при наличии реальной аудитории, которая хочет делиться ссылками на отфильтрованные списки.

**Оценка:** ~4 часа (History API + `filterLoads` из URL params при загрузке страницы).

**Условие реализации:** Когда появятся первые пользователи, которые явно просят «отправь мне список Тбилиси → Батуми рефрижераторы».

---

### 2.4.4 — Процесс верификации перевозчиков

**Описание:** Флаг `is_verified` на пользователе существует в БД и отображается в UI (✅ в модалке груза). Но процесса верификации нет — ни формы подачи документов, ни очереди рассмотрения, ни уведомлений.

**Что есть сейчас:** Admin endpoint `POST /api/users/admin/{id}/verify?secret=...` — Builder может вручную верифицировать пользователя через API.

**Почему отложено:** Первые клиенты — знакомые Тимура. Верификацию можно ставить вручную через admin API на первые 50 компаний.

**Оценка полной реализации:** ~2 дня (форма загрузки документов + очередь + email уведомления + admin panel).

**Условие реализации:** При 50+ зарегистрированных компаний, когда ручная верификация становится bottleneck.

---

## 🔄 В работе

_Текущий спринт — Трек 11 (финальная зачистка перед Категорией 3)._

---

## ✅ Реализовано

- ADR-006: Мультивалютность (GEL/USD, курс NBG)
- ADR-007: Города (автодополнение, нормализация)
- ADR-008: Неаутентифицированный доступ
- ADR-009: Тарификация и планы
- ADR-010: GDPR soft delete
- ADR-012: Demo-режим
- Трек 8: State machine (статусы, race condition protection)
- Трек 9: Справочники, счётчики шапки, языки
- Трек 10: Блокировка аккаунта, идемпотентность
- Трек 11: Demo mode, инвалидация кеша, верификация (admin API)

---

### 6.2.4–6.2.5 — Подписка перевозчика на маршруты (❌ не реализовано)

**Описание:** Перевозчик указывает маршруты (откуда/куда/тип кузова) и получает TG-уведомление при появлении нового подходящего груза.

**Почему важно:** Без подписок перевозчик должен вручную обновлять ленту. Это снижает активность и retention.

**Что нужно:**
- Таблица `route_subscriptions (user_id, from_city, to_city, truck_type)`
- Хук в `POST /api/loads/` — после создания груза искать совпадающие подписки → `notify_new_load()`
- UI: вкладка «Мои подписки» в кабинете перевозчика

**Оценка:** ~1 день (модель + хук + простой UI)

**Условие реализации:** До первой рекламной кампании или при первой жалобе перевозчика «не вижу новые грузы».

**Статус:** ❌ Не реализовано — ПРИОРИТЕТ перед рекламой.
