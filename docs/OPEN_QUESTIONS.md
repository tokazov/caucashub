# OPEN_QUESTIONS.md — Открытые архитектурные вопросы CaucasHub

Формат: вопрос → варианты → статус (открыт / решён).

---

## OQ-001: Процесс деплоя и миграций (ADR-011)

**Вопрос:** Какой процесс деплоя выбираем? Варианты A–E в `DECISIONS.md#ADR-011`.

**Статус:** OPEN — ожидает решения Тимура.

---

## OQ-002: org_type — свободная строка или Enum (1.3)

**Вопрос:** Юр. форма компании сейчас — свободная строка `VARCHAR`. Нужен ли Enum с фиксированным списком `(llc, ie, jsc, private)`?

**Риск:** Записи типа «ООО», «Ооо», «ooo» — разные с точки зрения БД. Экспорт в rs.ge некорректен.

**Статус:** OPEN.

---

## OQ-003: Валюта откликов — только GEL или dual-currency (1.6)

**Вопрос:** `Response.price_usd` — единственное поле цены в отклике. При сделке груз в GEL, отклик в USD. Что конвертировать при создании `Deal.agreed_price`?

**Текущее решение:** Конвертация через NBG курс в момент сделки (ADR-006). Это **реализовано**, но осознанно ли?

**Статус:** PROPOSED → требует явного подтверждения.

---

## OQ-004: Справочник городов — expand scope? (1.9.5)

**Вопрос:** Сейчас есть `GET /api/cities` (справочник Грузии). Но `from_city`/`to_city` принимают любую строку, в том числе международные (Москва, Ереван).

**Варианты:**
- A: Для local-грузов — только из справочника (`select`). Для intl — свободный ввод.
- B: Два отдельных поля: `from_city_id` (FK) для Грузии + `from_city_intl` (VARCHAR) для международных.

**Статус:** OPEN.

---

## OQ-005: Верификация перевозчиков — процесс (2.4.4)

**Вопрос:** `is_verified=True` устанавливается вручную через admin API. Достаточно ли это на первом этапе, или нужна форма подачи документов?

**Текущее решение:** Admin API `POST /api/users/admin/{id}/verify` — Builder устанавливает вручную.

**Статус:** OPEN — отложено в `ROADMAP.md#2.4.4` до 50+ компаний.

---

## OQ-006: URL-параметры с фильтрами (3.4.8)

**Вопрос:** Нужны ли shareable ссылки с фильтрами (например, `?from=Тбилиси&to=Батуми&type=ref`)?

**Статус:** OPEN — отложено в `ROADMAP.md#3.4.8` до появления пользователей, которые явно запрашивают.

---

## OQ-007: SMS-сервис для подтверждения смены телефона

**Вопрос:** Сейчас код подтверждения при смене телефона отправляется на email пользователя (компромисс). Оптимально — SMS на новый номер.

**Текущая реализация:** `POST /api/users/me/request-phone-change` → код на email. Работает.

**Что нужно для SMS:** Подключить SMS-провайдер (SMSAERO, Twilio, Magti/Silknet для Грузии). Стоимость: ~$0.05-0.10/SMS.

**Условие:** При достижении 50+ активных пользователей — подключить SMS вместо email.

**Статус:** OPEN — отложено до роста.

---

## OQ-009: UX-проверка перед запуском рекламы

**Вопрос:** Аудит Категория 6 проверяется через автоматизированные E2E-тесты. UX-сторона (понятность текстов, скорость UI, психологическая лёгкость) не будет проверена живым пользователем до запуска.

**Срочность:** Обязательно ДО первой рекламной кампании.

**Решение:** Тимур или приглашённый перевозчик/грузовладелец проходит сценарии A1–B4 из `UX_SCENARIOS.md` руками минимум за день до старта рекламы.

**Статус:** OPEN — блокер для рекламы.

---

## OQ-010: Внешний геокодер (Yandex/LocationIQ)

**Вопрос:** Запрос на Yandex Advanced не отправлен. Сейчас работает собственная таблица 45 городов. Для реальной биржи нужен геокодер с покрытием сёл и районов Грузии.

**Варианты:**
- **Yandex Advanced** — 3–7 дней на ключ, платно, хорошее покрытие Грузии/СНГ
- **LocationIQ** — 1 час на ключ, OSM-данные, бесплатно до 5000 запросов/день. Достаточно для старта.

**Срочность:** Обязательно ДО запуска рекламы — пользователи будут вводить не только города из таблицы 45.

**Статус:** OPEN — блокер для рекламы.

---

## Q-011: Машины (trucks) как сущность — нужны ли в текущей модели?

**Контекст:** На проде (май 2026): 30 пользователей, 4 отклика, 12 сделок, **0 машин**.
Перевозчики успешно откликаются без добавления машин. Раздел «Транспорт» в нижней навигации существует, но не используется.

**Гипотеза:** Машина не воспринимается как нужная сущность для подачи отклика. Пользователи проходят весь флоу без неё.

**Варианты решения:**
1. **Сделать машину обязательной для отклика** — перевозчик указывает номер/тип при подаче отклика
2. **Убрать раздел «Транспорт» из навигации** — до набора аудитории
3. **Переосмыслить как «профиль автопарка»** — добавляет доверие, но не блокирует отклик

**Срочность:** ДО запуска рекламы — определить, не запутает ли пустой раздел новых пользователей.

**Статус:** OPEN — решение за Тимуром.

---

## Q-012: 0 пользователей подключили Telegram-бот

**Контекст:** На проде 30 пользователей, никто не подключил Telegram-уведомления.

**Диагностика (2026-05-04):**

1. **CAUCASHUB_TG_BOT_TOKEN не задан в Railway ENV** → `BOT_TOKEN=""` в коде → вебхук регистрируется на `/api/tg/webhook/` (без токена), все апдейты от Telegram не принимаются.

2. **Бот @caucashub_bot** — токен нигде не задокументирован, в TOOLS.md отсутствует. Неизвестно был ли создан бот вообще.

3. **generate-link** работает (`https://t.me/caucashub_notify_bot?start=TOKEN`) — deeplink генерируется, но бот по умолчанию называется `caucashub_notify_bot` (из ENV fallback), а в emails/документации упоминается `caucashub_bot`. Несоответствие.

4. **Вебхук не установлен** — без токена `setWebhook` никогда не вызывался.

**Итог (исправлено 2026-05-04):** Telegram РАБОТАЕТ. Ошибка диагностики — токен находится в project 7bd0ba2e, а не fe9eead2. Бот @caucashub_bot активен, webhook установлен, /start обрабатывается корректно.

**Что нужно для починки:**
1. Создать бота через @BotFather → получить токен
2. Добавить `CAUCASHUB_TG_BOT_TOKEN=<token>` и `CAUCASHUB_TG_BOT_USERNAME=<username>` в Railway ENV
3. Вызвать `setWebhook` на `https://api-production-f3ea.up.railway.app/api/tg/webhook/<token>`
4. Проверить /start flow

**Срочность:** БЛОКЕР для Этапа 2 подписок (уведомления через TG). Починить до реализации матчинга.

**Статус:** OPEN — **блокер**.

---

## Q-012 Update (2026-05-04 15:00 UTC)

**Telegram РАБОТАЕТ.** Ошибка первичной диагностики — токен задан в project `7bd0ba2e` (caucashub), а не `fe9eead2` (endearing-communication).

Проверено:
- `getMe` → ok, бот `@caucashub_bot` (id: 8527781523) активен
- `getWebhookInfo` → webhook установлен на `https://api-production-f3ea.up.railway.app/api/tg/webhook/<token>`
- Тест `/start TOKEN` → `telegram_id` сохранился в БД, `linked: true`

**Реальная причина 0 подключений:** пользователи не нажали «Подключить Telegram» в настройках. UX задача — сделать более заметным.

**Q-012 статус:** RESOLVED технически. Открыт как UX задача.

---

## Q-014: Геокодер — фронт ещё использует старые CITIES, не подключён к LocationIQ

**Контекст:** LocationIQ подключён (2026-05-04), ключ `pk.e87...` в Railway ENV.
Эндпоинт `GET /api/cities/search?q=...&lang=ru` работает.

**Открытые вопросы:**
1. **Фронт не использует геокодер** — поля «Откуда»/«Куда» в форме груза пока работают как свободный текст или выбор из `CITIES` массива. Нужно подключить `/api/cities/search` как autocomplete.
2. **Первый результат не всегда правильный** — «Натахтари» возвращает верный village первым, но pub в Рустави тоже присутствует. Нужна фильтрация по `type` (village/city/town/suburb — да, pub/restaurant/shop — нет).
3. **Нормализация названий** — при сохранении груза `from_city` пишется как введёт пользователь. После подключения геокодера нужно стандартизировать: сохранять `name_local` (грузинское) или `name_en` (английское транслитерированное)?
4. **Матчинг подписок** — сейчас матчинг через `from_city.lower() == sub_city.lower()`. После геокодера: «Тбилиси» vs «Tbilisi» vs «თბილისი» — три разных строки, матч не сработает.

**Срочность:** Перед запуском рекламы — autocomplete должен работать, иначе пользователи будут вводить разные варианты одного города.

**Статус:** OPEN — требует реализации фронта + нормализации при сохранении.

---

## Q-013: Регистрация сторонних сервисов — границы автономии ассиста

**Контекст:** В ходе работы над LocationIQ (2026-05-04) ассист зарегистрировал аккаунт
на `caucashub.geo@gmail.com` (несуществующий email — опечатка в задании).
Это создало неясность: на чей email регистрировать production-сервисы?

**Правило (принято 05.05.2026):**
- **Внешние production-сервисы** (LocationIQ, Brevo, Cloudflare и т.д.) — регистрирует **Тимур** на свой email.
- **Ассист** может тестировать через временные dev-аккаунты в рамках одной сессии.
- **Ассист не создаёт** production-аккаунты и не хранит чужие credentials.

**Статус:** RESOLVED — правило зафиксировано.

---

## Q-015: Раздел «Транспорт» → полноценная биржа транспортных предложений

**Контекст:** Q-011 (машины) поставил вопрос: оставить, убрать или развить?

**Решение Тимура (05.05.2026):** Развить в полноценную симметричную биржу.
Перевозчик размещает TransportOffer (маршрут, дата, тип, цена), грузовладелец откликается —
создаётся сделка. Архитектурно симметрично грузовой бирже.

**Реализуется:** Часть 3 текущего задания (ADR-016 → Этапы 1–5).

**Статус:** RESOLVED — двусторонняя биржа реализована (ADR-016, Этапы 1–8, 2026-05-05).

---

## Q-016: Протокол подтверждения ADR — правило (принято 05.05.2026)

**Контекст:** Дважды нарушен протокол:
1. Регистрация LocationIQ на `caucashub.geo@gmail.com` — ассист создал production-аккаунт самостоятельно.
2. ADR-016: ассист написал «СТОП — жду OK» и через одно сообщение сам начал реализацию без подтверждения.

**Правило (жёсткое, с 05.05.2026):**

> Архитектурные решения (ADR) реализуются **только** после явного письменного подтверждения от Тимура через Claude. Молчание не равно согласию.

- STOP в задании = блок, не пауза для проформы.
- Если нужно подтверждение по ADR-NNN — ждёшь буквальной фразы вида **«ADR-NNN принят, реализуй»**.
- Если 24 часа прошло без ответа — пингуешь: «Жду решение по ADR-NNN».
- До подтверждения: документация, changelist, архитектура — OK. Код — нет.

**Текущий статус ADR-016:** Этап 1 (модели/миграции) закоммичен — полезен и не откатывается. Этапы 2–8 — заморожены до явного «ADR-016 принят, реализуй» от Тимура.

**Статус Q-016:** RESOLVED — правило зафиксировано.

---

## Q-017: Процесс миграций PostgreSQL enum — пробел (2026-05-05)

**Контекст:** После добавления `LoadStatus.completed` в Python enum (`app/models/load.py`)
деплой прошёл успешно, но первый же UPDATE с `status='completed'` вернул 500:
`invalid input value for enum loadstatus: "completed"`.

### Что произошло (разбор)

1. **Alembic-миграция добавляющая `completed` в enum loadstatus — не была написана.**
   Добавлено только в Python-объект `LoadStatus(str, enum.Enum)` — это не меняет схему PostgreSQL.

2. **Alembic не генерирует `ALTER TYPE ... ADD VALUE` автоматически.**
   При `alembic revision --autogenerate` изменения в `Enum` не обнаруживаются.
   Это известный limitation SQLAlchemy+Alembic для PostgreSQL native enums.

3. **Emergency migrations (main.py) исправили ситуацию** — но только после того как падение уже произошло.

4. **ADR-011 Вариант D (healthcheck `/health`) не поймал 500.**
   `/health` проверяет `SELECT 1` — соединение с БД. После деплоя он возвращал 200.
   500 возникал только при конкретном UPDATE с новым enum-значением, не при старте сервиса.
   Вариант D ловит краши при запуске, но не ловит runtime-ошибки в редко используемых эндпоинтах.

### Системный фикс (план)

**Вариант 1 — Всегда писать явную Alembic-миграцию при изменении enum:**
```python
# В новой миграции:
def upgrade():
    op.execute("ALTER TYPE loadstatus ADD VALUE IF NOT EXISTS 'completed'")
```

**Вариант 2 — Перейти с PostgreSQL native enum на VARCHAR:**
```python
# В модели:
status = Column(String(20), default='active', index=True)
# Проверка через Python-валидацию, не БД
```
Плюс: новые значения не требуют миграции. Минус: нет DB-level constraint.

**Вариант 3 — Добавить шаг проверки в pre-deploy:**
В `nixpacks.toml` в `[phases.migrate]` добавить проверку enum перед `alembic upgrade head`:
```bash
python -c "from app.models import *; print('enum check OK')"
```
Это не ловит все случаи, но хоть что-то.

**Рекомендация:** Вариант 1 (явные миграции) + добавить в checklist разработки:
> При изменении `class XxxStatus(str, enum.Enum)` → написать Alembic-миграцию с `ALTER TYPE`.

**Статус:** OPEN — требует выбора варианта и реализации.

---

## Q-018: Healthcheck — расширен до smoke-тестов (2026-05-05)

**Контекст:** ADR-011 Вариант D (auto-rollback по healthcheck) не поймал падение 500
после добавления нового enum-значения. `/health` проверял только `SELECT 1`.

**Решение (реализовано 2026-05-05):**
`/health` теперь делает smoke-тесты трёх критических эндпоинтов:
- `GET /api/loads/?limit=1` — биржа грузов работает
- `GET /api/cities/search?q=Tbilisi&lang=en` — геокодер работает
- `GET /api/dictionaries/truck-types` — справочники работают

При любом FAIL → `/health` возвращает **503** с описанием какой именно чек упал.
Railway auto-rollback теперь реально ловит миграционные и API-ошибки.

**Ограничение:** Smoke-тесты пропускаются (skip) если localhost недоступен на старте
(первые секунды до полного запуска uvicorn). Railway делает healthcheck через ~15 сек.

**Статус:** RESOLVED.

---

## Q-019: Yandex Maps SDK — статус ключа и нагрузка

**Контекст:** Карты на caucashub.ge используют Yandex Maps JS API 2.1.

```html
<script src="https://api-maps.yandex.ru/2.1/?apikey=aef19e33-8ea1-4039-8353-2f0df688664a&lang=ru_RU" async></script>
```

**Ответы на вопросы:**

1. **Ключ:** `aef19e33-8ea1-4039-8353-2f0df688664a` — получен 27 марта 2026 на `t.tokazov@gmail.com` через [developer.tech.yandex.ru](https://developer.tech.yandex.ru). Зарегистрирован на email Тимура ✅ (Q-013 не нарушен).

2. **Whitelist доменов:** `caucashub.ge` и `www.caucashub.ge` добавлены 31 марта 2026. На `caucashub.pages.dev` карта НЕ работает (домен не в whitelist) — это ожидаемо.

3. **Тариф:** Бесплатный тариф Maps JavaScript API — до **25 000 загрузок/день** для некоммерческих сайтов. При превышении — карты перестают рендериться.

4. **async тег:** Ключ загружается с `async`. По опыту 31 марта это вызывало `ymaps is not defined` ошибки. В коде карты есть `setTimeout(_initMap, 300)` и проверка `typeof ymaps === 'undefined'` как защита — но это хрупко.

**Риски:**
- При 100+ уникальных посетителей в день — потенциальный rate limit (25K/день = ~17 посетителей в минуту при средней нагрузке).
- `async` загрузка может давать race condition на медленных соединениях.

**Рекомендуемые действия:**
1. Тимуру зайти на [developer.tech.yandex.ru](https://developer.tech.yandex.ru) → проверить лимиты и статус ключа.
2. Рассмотреть переход на коммерческий тариф если нагрузка вырастет.
3. Убрать `async` → заменить на обычный `<script>` или использовать `ymaps.ready()` callback правильно.

**Статус:** OPEN — требует проверки Тимуром на developer.tech.yandex.ru.
---

## Q-021: Верификация браузерного анализа
**Дата:** 2026-05-05

Перед любым выводом о runtime-поведении кода в браузере:
1. Hard reload (Cmd/Ctrl+Shift+R) — сбросить кеш
2. Проверить через `document.querySelectorAll('script[src]')` или Network tab, что версии файлов соответствуют проду
3. Только после этого делать выводы

**Node-тесты и статический анализ не заменяют проверку в реальном браузере.**

**Причина правила:** 2026-05-05 — инструментный браузер кешировал промежуточную версию api.js (без дубликатов), что привело к ошибочному выводу что функция = обёртка. В реальном Chrome на проде функция была рабочей.
---

## Q-023: renderTrucks падает на innerHTML null при showCabinet
**Дата:** 2026-05-05  
**Статус:** открыт, не фиксить сейчас

`renderTrucks` в main.js:823 пытается установить `innerHTML` на `null` — элемент не найден через `getElementById`.  
Стек: `renderTrucks → applyLang → switchCabTab → showCabinet`  
Не связан с картой. Разобраться отдельно.

---

## Q-024: Дубликаты в api.js — НЕ УДАЛЯТЬ
**Дата:** 2026-05-05  
**Статус:** зафиксировано, не трогать

Две попытки удалить дубликаты `openRouteMap` из api.js привели к поломке карты.

**Корневая причина:** функция `openRouteMap` в main.js использует глобальную переменную `_routeMap`, которая объявляется в блоке api.js:299-347 (`var _routeMap = null`). Это зависимость между файлами.

**Правило:** при следующих правках карты — НЕ трогать api.js.  
Если нужна чистка — сначала добавить `var _routeMap = null` в начало блока карты в main.js, убедиться что карта работает, только потом убирать из api.js. Делать только при острой необходимости, отдельным коммитом.

---

## Q-022: Дубликаты openRouteMap в api.js — оставлены сознательно (НЕ УДАЛЯТЬ)
**Дата:** 2026-05-05  
**Статус:** закрыто навсегда — не трогать

Две попытки удалить дубликаты привели к поломке карты.

**Корневая причина:** функция `openRouteMap` в main.js:4286 использует глобальную переменную `_routeMap`, которая объявляется как `var _routeMap = null` в блоке api.js:299-347. Это работающий механизм через неявное связывание глобальных переменных. Технически некрасиво, но функционально стабильно.

**Правило:** при следующих правках карты — НЕ трогать api.js.  
Если нужно убрать дубликаты — сначала добавить `var _routeMap = null` в начало блока карты в main.js, проверить в браузере, потом удалять из api.js. Только при острой необходимости, отдельным коммитом.